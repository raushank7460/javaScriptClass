<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram Profile</title>

    <style>
        body {
            font-family: Arial;
            background: #fafafa;
            padding: 20px;
        }

        .profile {
            background: white;
            padding: 20px;
            border-radius: 8px;
            width: 400px;
        }

        img {
            width: 100%;
            border-radius: 10px;
            margin-top: 10px;
        }

        button {
            padding: 8px 16px;
            margin-top: 10px;
            border: none;
            background: #3897f0;
            color: white;
            cursor: pointer;
        }
    </style>
</head>

<body>

<div class="profile">
    <h2>@<%= data.name %></h2>
    <p>Followers: <%= data.followers %></p>
    <p>Following: <%= data.following %></p>

    <button>Follow</button>
    <button>Message</button>

    <hr>

    <h3>Posts</h3>

    <% data.posts.forEach(post => { %>
        <img src="<%= post.image %>">
        <p>❤️ Likes: <%= post.likes %> | 💬 Comments: <%= post.comments %></p>
    <% }) %>
</div>

</body>
</html>



